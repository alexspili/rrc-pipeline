# Runtime
requests>=2.31          # fetch.py
pillow>=10              # page image downscale (bilevel -> grayscale, area filter)
anthropic>=1.0          # page classification (Haiku), extraction (Sonnet)

# Matching two pages by the marks on the paper: pipeline/paper.py and the
# probes under scripts/. Outline profiles, correlation across a fixed
# transform, and connected-component labelling are all array work, and hand
# rolling them is a great deal of unreviewable code inside a module whose only
# output is a similarity score that nobody can eyeball. Added, then removed
# with an abandoned experiment, then re-imported without being re-declared;
# tests/tier2/test_requirements.py now stops that recurring (DEFECTS #52).
numpy>=2.0              # array maths for pipeline/paper.py
scipy>=1.11             # ndimage: connected components, morphological opening

# Build-time Textract read for the stage-five template probe
# (docs/labeling-protocol-extract.md). Imported ONLY by
# scripts/textract_read.py; nothing in pipeline/ touches it, so the
# one-API-key runtime property stands. Declared before first import
# (DEFECTS #52).
boto3>=1.34

# Test runner
pytest>=8

# Undefined-name check, tests/tier2/test_names_resolve.py. A call added inside
# a function body passes every import-shaped test and fails at runtime; commit
# ce99e47 shipped exactly that (DEFECTS #70). Pure Python, no transitive deps.
pyflakes>=3.2

# Tooling. openpyxl builds the labelling workbook (scripts/make_label_workbook.py);
# nothing in pipeline/ imports it and the tests skip if it is absent.
openpyxl>=3.1

# System dependency, NOT pip-installable: poppler (pdfimages, pdftotext).
#   macOS: brew install poppler
# pipeline/render.py preflights for it and fails with this hint.
