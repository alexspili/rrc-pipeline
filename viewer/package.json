{
  "name": "rrc-span-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Static viewer for the extraction bundle: page images, span overlays at three honesty tiers, findings, client-side search. No backend, no hosted inference (rejected deliberately: cost, injection, uptime).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
